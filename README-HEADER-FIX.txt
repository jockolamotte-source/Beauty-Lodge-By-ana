Header layout correction

This build fixes the duplicated stacked appearance by overriding the original
.brand flex-direction rule. The circular badge and Beauty Lodge / by Anna text
now render as one compact horizontal logo unit.

Upload all files over the matching repository files.
